{
  "name": "@monorepo/api",
  "version": "1.0.0",
  "scripts": {
    "start": "node dist/index.js",
    "check-types": "tsc",
    "build": "tsc --build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@monorepo/core": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.2"
  }
}
