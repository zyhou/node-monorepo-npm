{
  "name": "@monorepo/api",
  "version": "1.0.0",
  "scripts": {
    "check-types": "tsc",
    "compile": "tsc --build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@monorepo/core": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.2"
  }
}
